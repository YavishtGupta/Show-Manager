Requirments: MySQL and Python installed on the device
             Pandas (type in command prompt/terminal <pip install pandas>)
             My Sql Connector (type in command prompt/terminal <pip install mysql-connector-python>)
             (ps- exclude the <>)
